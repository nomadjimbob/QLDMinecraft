region_restricted_data:
    type: custom
    command: rg
    action: test
    aliases:
        - tester
    script:
        - narrate hiiii
    events:
        on makeup:
            - narrate byeeeee


region_restricted:
    type: world
    debug: false
    events:
        on load:
            - run qm_command.add.option def:region_restricted_cmd|<list[rg|region]>|restricted

#         # on player enters region:
#         #     - if <yaml[regions].read[<context.name>.type]||null> == restricted:
#         #         - determine passively actionbar:restricted
#         #         - determine passively message:restricted2
#                 # - determine cancelled
#         # on player places block:
#         #     - if <proc[qmp_region.get_type].context[<context.location>]> == town:
#         #         - determine cancelled
            
            
