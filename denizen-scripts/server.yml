npcchat:
  type: format
  format: "<&b><npc.name><&f><&co> <&a><text>"

serverchat:
  type: format
  format: "<&6> <text>"

servernotice:
  type: format
  format: "<&6>Server<&co> <text>"

servererror:
  type: format
  format: "<&c>Server<&co> <text>"

#doubledoors:
  #type: world
  #events:
    #on player right clicks block:
    #- define loc <context.location.block>
    #- if <[loc].material.name.ends_with[door]>:
    #  - if <[loc].add[-1,0,0].material.name.ends_with[door]>:
    #    - switch <[loc].add[-1,0,0]>
    #  - if <[loc].add[1,0,0].material.name.ends_with[door]>:
    #    - switch <[loc].add[1,0,0]>
    #  - if <[loc].add[0,0,-1].material.name.ends_with[door]>:
    #    - switch <[loc].add[0,0,-1]>
    #  - if <[loc].add[0,0,1].material.name.ends_with[door]>:
    #    - switch <[loc].add[0,0,1]>
